# Claude Code local session / settings
.claude/

# Public student-site build output (its own git repo -> sim2field-book); built by
# build/publish-student.js, never tracked in this private repo.
dist-public/

# OS / editor junk
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*~

# Note: build/node_modules is intentionally vendored (exact-pinned KaTeX for the
# drift guard; offline builds) and is committed on purpose - do not ignore it.
